-name: Install Nginx software
 hosts: web
 tasks: #installing the nginx software
    -name: Install the Nginx software
     ansible.builtin.package:
      name: nginx
      state: present

    -name: enable the nginx service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes

