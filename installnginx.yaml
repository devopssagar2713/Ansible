- name: nginx install and run
  hosts: web
  tasks: 
   - name: nginx install
     ansible.builtin.package:
       name: nginx
       state: present
   
   - name: run nginx and enable
     ansible.builtin.service:
       name: nginx
       state: started
       enabled: yes
